apiVersion: v1
kind: ConfigMap
metadata:
  name: pal-tracker
  labels:
    app: pal-tracker
data:
  welcome.message: "hello from kubernetes post preStop hook"
  management.endpoints.web.exposure.include: "info,health,palTrackerFailure"
